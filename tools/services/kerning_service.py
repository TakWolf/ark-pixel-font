from pixel_font_knife.glyph_file_util import GlyphFlavorGroup


def calculate_kerning_pairs(context: dict[int, GlyphFlavorGroup]) -> dict[tuple[str, str], int]:
    latin_code_points = set()

    kerning_pairs = {}

    return kerning_pairs
