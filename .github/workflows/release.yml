name: Release

on:
  release:
    types: [published]

jobs:
  release:
    permissions:
      contents: write
    strategy:
      matrix:
        font-size: [10, 12, 16]
        width-mode: [monospaced, proportional]
        font-format: [otf, otf.woff, otf.woff2, ttf, ttf.woff, ttf.woff2, ms.bitmap.ttf, otb, dfont, bdf, pcf]
      fail-fast: false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0
      - name: Setup uv
        uses: astral-sh/setup-uv@v8.3.2
      - name: Install dependencies
        run: uv sync
      - name: Build
        run: uv run -m tools.cli --cleanup --font-sizes ${{ matrix.font-size }} --width-modes ${{ matrix.width-mode }} --font-formats ${{ matrix.font-format }} --attachments release
      - name: Release
        uses: softprops/action-gh-release@v3.0.2
        with:
          files: ./build/releases/*.zip
